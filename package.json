{
  "name": "dtc-broken-plumbing",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "deploy": "npm run db:migrations:apply && wrangler deploy",
    "db:migrations:apply": "wrangler d1 migrations apply DB --remote",
    "dev": "wrangler dev",
    "start": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.95.0",
    "typescript": "^5.5.0",
    "@cloudflare/workers-types": "^4.20260101.0"
  }
}
